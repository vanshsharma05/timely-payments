/**
 * The one look for a text field and its label inside a dialog.
 *
 * Five dialogs carried five copies of the same class string, with paddings
 * and radii that had drifted; the cheque dialog's pair is the one they all
 * use now.
 */
export const FIELD = 'w-full h-10 px-3 bg-gray-50 dark:bg-gray-800 border border-gray-300 dark:border-gray-700 rounded-lg text-[13.5px] text-gray-900 dark:text-white focus:ring-2 focus:ring-accent/40 focus:outline-none disabled:opacity-60 max-md:h-11';
export const LABEL = 'block text-[11.5px] font-bold text-gray-600 dark:text-gray-400 uppercase tracking-wider mb-1';
