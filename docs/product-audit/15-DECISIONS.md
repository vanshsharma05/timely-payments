# 15 — DECISIONS

| # | Date | Decision | Why | Alternatives considered |
|---|---|---|---|---|
| D1 | 2026-09-16 | Audit work continues on the existing `restore-and-fix` branch with `main` fast-forwarded after each push, because the owner has asked that every finished update be pushed live. Larger refactor batches (e.g. splitting `App.tsx`) get their own short-lived branch and are merged only after the browser suites pass. | Owner's standing instruction; one contributor, no CI — a long-lived improvement branch would only diverge. | Dedicated `audit/*` branch for everything — rejected for now. |
| D2 | 2026-09-16 | Phase 0 makes no application code changes. The two production `npm audit` findings (nodemailer, qs) are P2 with non-breaking fixes and are scheduled for the first Phase 8/12 batch rather than done blind now. | Protocol: no code changes in Phase 0 unless P0. | Fix immediately — deferred one session. |
| D3 | 2026-09-16 | `/ARCHITECTURE.md` stays the engineering handbook; `docs/product-audit/` is the audit's own record and points at it rather than duplicating it. | Avoid two competing sources of truth. | Merge the two — rejected. |
| D4 | 2026-09-16 | No code changes in Phase 1–2; the P1 findings (SEC3 reset by Manager, R1 concurrent overwrite) are recorded for the security/reliability phases, not patched now. | Both need a design decision (Q8; a version column or realtime) and tests that need staging (Q6). Neither is a live exploit today (internal staff, sign-up disabled). | Hot-fix SEC3 by hiding the reset from Managers — deferred; wants the owner's answer to Q8 first. |
| D5 | 2026-09-16 | `bun.lock` stays until the tooling cleanup phase (owner's instruction). | | |
| D6 | 2026-09-17 | Evidence for "what the app writes" is gathered with network-layer request interception (aborting mutations) rather than a staging project, until Q6 is answered. | Zero production risk; the payload is exactly what would have been sent. | Creating throwaway records — rejected for customer rows. |
| D7 | 2026-09-17 | The recommended remediation for R1/processStatuses is Option A (partial column writes) + Option B (stop persisting derived status) + the C1 fix, to be implemented only after unit tests for `outstandingToRow`/diffing and the browser suites are runnable. Optimistic concurrency (C) later; realtime (D) optional. | Smallest change that removes the mechanism; no DB migration; fits the current architecture. | Realtime-first — more infrastructure without fixing the write pattern. |
| D8 | 2026-09-17 | C1 is fixed inside the dialog (spread the existing record; write owner/date/money only when the field changed), not by moving the save logic into a helper. The unit tests exercise the real component through jsdom. | Smallest change; the component stays the single place that knows its form; the test proves the shipped code, not a copy. | A pure `applyCustomerEdits()` helper — rejected for now; can be extracted in the App.tsx phase if the dialog grows. |
| D9 | 2026-09-17 | When an Admin intentionally edits a money figure, the dialog keeps its pre-existing recomputation (types = total's type, roll-ups summed). Pinned by a test, not endorsed; Q10 decides whether the inputs should exist at all. | The session's brief: no financial-rule changes. | Netting the buckets in the dialog too — deferred to Q10. |
| D10 | 2026-09-17 | Option A is implemented inside the existing `useCollectionSync` (an optional `partial` capability) rather than a second hook or a customer-specific hook, with the diff (`customerRowDiff`) and the write (`updateCustomerColumns`) in the repository. Whole-row `updateCustomers` is removed — it was the defect. | One write path, one baseline model, the cheque/template hooks untouched; the diff and the write are pure/testable. | A generic "partial upsert" for every table — rejected (customers are the only table with the problem and the only one with a typed row). |
| D11 | 2026-09-17 | R1 is tracked as R1-A (fixed), R1-B (same-field conflict, open) and R1-C (derived status persisted, reduced). Option A is **not** recorded as "concurrency solved". | The three have different fixes and different risks. | — |
| D12 | 2026-09-17 | Option B keeps the `status` column and keeps user actions writing a snapshot word into it; only the automatic rewrite (`processStatuses`) is removed and every reader derives. | Smallest change that ends the redundant writes; no migration; `Completed` — the one declared value — needs the column anyway. | Narrowing the column to a `collected` flag now — deferred (schema change, no staging). |
| D13 | 2026-09-17 | Final status invariant: `customers.status` records only whether the money was collected (`Completed`); reopening writes `Pending`; no writer stores a derived follow-up word and no reader consults one (the no-date fallback is removed). | The last derived write was the follow-up dialog's default path, which the probe caught; the fallback was the last derived read (0 rows used it). | Keeping snapshot words as an audit trail — rejected: nothing reads them and they mislead anyone querying the table. |
| D14 | 2026-09-17 | R1-B is designed (11 §2.2) and not implemented: field-aware compare-and-set on Option A's baseline is recommended over row-level `updated_at` OCC; the owner decides the field policy and the conflict experience (19 Q15) first. | Row-level OCC rejects unrelated-field edits after every bulk event (3,235 / 504 rows in a day); field-level needs no new state. | Row-level OCC (conventional, wrong granularity); realtime first (reduces but does not prevent). |
| D15 | 2026-09-17 | The fresh start is a database function (`reset_book`, one transaction, snapshot first, restore function), planned in the browser with the sync's own merge; it keeps every account, its id, owner, collector, contacts and thread; unlisted accounts are settled, never deleted; cheques and follow-up work are cleared as before; **Admin only** (owner's decision the same day; Manager was the previous state); restore is Admin-only; owners/collectors and the activity history are kept by decision, not just by default. | Safety (T1–T6 in 11 §3.2) required moving it server-side and ending the deletes; the smallest robust transactional unit in this stack is a Postgres function called over RPC — no new Vercel route, no service role in the browser, RLS untouched. Keeping owners/threads is the safe default pending Q8. | A Vercel `/api/reset` with the service role (not transactional across statements); removing the feature (Q8 (c), owner's call); Admin-only now (a policy change). |
| D16 | 2026-09-17 | Failed saves: the record still goes into the book at once (the tab keeps it and retries), but dialogs wait for the server's verdict through `flush()` and stay open on a refusal; success is said only after acceptance; refusals are retried with backoff and on focus/online, and stay visible in the header and banner until saved. Stale tabs: a pending-aware refetch on focus/interval/reconnect/button, no Realtime. | Honest without changing the write architecture (state → diff → hook) or adding infrastructure; the unsaved change is never lost and never overwritten. | Awaiting the write *before* applying it to state (loses the offline-tolerant retry); Supabase Realtime (infrastructure, not needed at this scale). |
| D17 | 2026-09-17 | Phase 4 begins with the CRM daily workflow only: the follow-up dialog leads with the outcome and folds the rest; the dialog steps through the list it was opened from; the book's overview folds by role; rows are two fixed lines; delete appears on hover; every dialog closes on Esc. No new visual style — the existing tokens and primitives. | The daily loop (find → open → log → next) is where the time goes; a whole-app redesign before it is measured would be guesswork. | Redesigning every screen at once (rejected: scope); a new Dialog primitive migrating all eight dialogs (deferred: U10). |
