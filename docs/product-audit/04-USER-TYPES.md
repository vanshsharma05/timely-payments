# 04 — USER TYPES

Status: NOT STARTED — Phase 2. Roles known to exist (from `types.ts` `UserRole` and `ARCHITECTURE.md` §5.1): Admin, Manager, CRM, Collector, Viewer, each with per-login permission overrides and a data-visibility setting (own accounts / assigned CRMs / whole book).

For each, fill the template from the protocol (who, goals, what to see first, common/infrequent/high-risk actions, likely confusion, feedback, device context, success/failure, first-time vs power-user needs).
