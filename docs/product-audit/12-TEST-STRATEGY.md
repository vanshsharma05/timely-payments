# 12 — TEST STRATEGY

Status: unit layer started 2026-09-17 (fourth session); the full strategy is written in Phase 13.

## Unit / regression layer (new)

- **Runner**: Vitest 3.2.7 (`vitest.config.ts`, node environment; a file that renders components declares `// @vitest-environment jsdom`). Dev dependencies added: `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/dom`. No production dependency changed.
- **Commands**: `npm run test:run` (once) · `npm test` (watch). Needs nothing but `npm install` — no Supabase, no sheet, no sign-in (`supabaseClient` sees no `VITE_*` env and never creates a client).
- **Files**: `tests/fixtures.ts` (synthetic accounts and users — `mixedAccount()` carries a collector, a Cr bucket among Dr ones, sheet-netted roll-ups, a settlement stamp, a declared rank, a forecast and a follow-up) · `tests/money.test.ts` (23 characterisation tests pinning M1–M8: `parseAmountAndType`, `netRollUp`, `overdueAgeing`, rank/bad debt, `hasOutstanding`, settlement, `mergeWithExistingFollowUps`, `processStatuses`, `chequeState`, the row mappers) · `tests/customerEditModal.dom.test.tsx` (13 C1 regression cases against the real dialog: no-change Save as CRM and Admin, the four corruption modes, collected-stays-collected, contact-only edits ×3, urgent, rank, note, changed date, an Admin's intentional money edit).
- **Result**: 36/36 pass at the C1 fix; 11 of the 13 dialog cases failed on the code before it.
- **Option A layer (2026-09-17, fifth session)**: `tests/customerRowDiff.test.ts` (11 — cases A–E, J of the diff contract, id never carried, key-order stability, money-only sync diff, status flip, ordered notes) · `tests/updateCustomerColumns.test.ts` (5 — exact body against a fake client, `null` kept, nothing sent for `{}`, stray `id` dropped, DB error surfaced) · `tests/useCollectionSync.dom.test.tsx` (12 — the real hook with fake timers: A no change, B one column, C two columns, D null, F failed write keeps the baseline and the retry carries the unsaved difference, G success advances, H sequential writes carry only the new change, I two customers independent, J sensitive fixture, new id via upsert then column writes, partial failure isolated per row, removal, status flip one column) · `tests/syncFlows.test.ts` (13 — follow-up save, activity mirror, owner/collector/rank/category, bulk date, `processStatuses` across midnight, balance sync: changed rows money-only, unchanged rows nothing, settled rows nil+stamp+urgency, new names via upsert). **77/77.**
- **Option B layer (2026-09-17, sixth session)**: `tests/derivedStatus.dom.test.tsx` (16 — A the reading moves with the date while the row does not; B `processStatuses` diffs to nothing for date-crossed rows; C the real hook with 172 stale rows and the clock moved to the next morning: one urgency toggle → exactly `{is_urgent}`; D Completed / Bad / settled unchanged; E eight boundary cases incl. the no-date and invalid-date fallbacks; F the book's Status counts fresh vs stale). Three older cases re-pinned to the new contract (`processStatuses` normalises dates only; the bulk tool computes its own word). **93/93.**
- **Status-contract layer (2026-09-17, seventh session)**: `tests/statusContract.dom.test.tsx` (15 — the real follow-up dialog and the real edit dialog driven in jsdom, the exact PATCH columns of each Save: B plain Save / urgency / new date / no follow-up / edit-dialog date send no `status`; C collected persists `Completed`; D Completed survives the calendar; E every reopen path pinned; F Reports categories and the server digest (`buildDigests` with a fake client) on stale rows). Three older cases re-pinned (no fallback to the stored word; the edit dialog and the bulk tool write no derived word). New probe `scripts/tests/status-contract-probe.cjs`. **109/109.**
- **Fresh-start layer (2026-09-17, ninth session)**: `tests/resetPlan.test.ts` (10 — the plan keeps ids incl. legacy `out_*`, carries money and settlement only, settles unlisted, adds new names unassigned, counts; backup file), `tests/resetSql.test.ts` (24 — `reset_book()` / `restore_book_backup()` run in **PGlite** (Postgres in WebAssembly, in-process) against the real `supabase/schema.sql` + `reset.sql` with a synthetic book: ids kept, threads/owners/collectors/PAN kept, follow-up work cleared, money applied, defaults restored, snapshot with who/counts, roles, phrase, stale-plan refusals, **transaction rollback when the last step fails**, restore incl. its own pre-snapshot), `tests/resetConfirmModal.dom.test.tsx` (6 — counts shown; button gated on backup + exact phrase; refusal shown in place). Harness: `tests/pglite.ts` (dev dependency `@electric-sql/pglite`). **149/149.** Nothing in the suite can reach the production project.
- **Reliability layer (2026-09-17, eleventh session)**: `tests/saveFailures.dom.test.tsx` (13 — the hook: `flush()` reports the refusal per row and keeps it pending; automatic retry with backoff until accepted; retry on `online`/`focus`; `retry()` carries a later edit with the first; `accept()`/`forget()` move the baseline without writing or deleting. The three dialogs on a refusal: open, reason shown, typed values kept, Save re-enabled; close on acceptance and for older callers. `SaveStatus`: saved / saving / not saved + Retry, freshness, `combineStatus`), `tests/refreshMerge.test.ts` (5 — server wins, pending kept, deleted-elsewhere dropped unless pending, created-here kept, accepted set). **167/167.**
- **Retry idempotency (2026-09-17)**: `tests/retryIdempotency.dom.test.tsx` (7 — a fake client that stores the row and then loses the answer: `addActivity` with a client id recovers the stored entry on the retry; the composer keeps one id across Posts; the cheque and edit dialogs keep one id across Saves; the hook's upsert of a new cheque / new customer lands once; a column update retried is byte-identical). **174/174.**
- **CRM workflow layer (2026-09-17, twelfth session)**: `tests/crmWorkflow.dom.test.tsx` (16 — the follow-up dialog's header states the account; the outcome block precedes the folded sections; each fold's summary; prev/next by button and Alt+arrows with the position shown and the ends greyed; Esc on all four dialogs; `Disclosure`; `followUpWhen` wording for no date / today / tomorrow / in 7d / far / overdue / collected). Two older cases re-pinned to the button's new name. **190/190.**
- **Decisions + QA (2026-09-17, thirteenth session)**: `crmWorkflow` +5 (one search: the book shows and edits the app bar's term, notes match; WhatsApp: the entry says opened, never sent; the dialog's link records it), `statusContract` E re-pinned (+1: a collected account opens on *Keep as collected*, a plain Save leaves it collected, *Follow up again* reopens). **196/196.**
- **Manager + Reports (2026-09-17, fifteenth session)**: `searchScope` 9 (the app bar's term is the customer search on every tab but Live stock), `managerReports` 16 (Reports speaks the book's words and shows the book's follow-up wording; each state counted once, Unattended = overdue + no follow-up, never bad debt; the Overdue tile and chip agree both ways; an ageing band and its chip agree; Reset clears state, ageing and search; Reports follows `initialCrmFilter`/`initialCategoryFilter`/`initialAgeingFilter` and re-filters on change; a CRM stays on their own book with no Excel; *Needs attention* only while open; one search in and out; 50 rows then *Show more*; name and *Follow up* both open the account; the team table's name → `(crmId, undefined)`, each count → its category, a collector's row has no buttons, most-unattended first, headers match Reports, no handler → no buttons). **221/221** (222 with the T51 test).
- **Cheques (2026-09-17, seventeenth session)**: `chequeRegister` 27 (one vocabulary for the six states and the retired `DueToday` reads as pending; `chequeWhen`; the register's order; the old filter names map to states; the strip's counts and *In hand*; *Upcoming* lists only the waiting ones; arriving with `today` / for one customer; search by customer, number, bank, note, amount; badge and dated line agree; actions per state and what each sends; the name opens the account; a Viewer sees no actions; a refused save is said on the row; delete asks with the cheque named, Cancel/Esc do nothing, *Delete cheque* deletes; a selection's four states and named delete; loading / empty / no-match; fifty rows; the dialog opens empty and refuses without a customer; the picker; a fixed customer; the duplicate warning; the date's wording and warning; the payload shape, status and one id for retries; edit prefill keeps *added by*). `retryIdempotency` and `saveFailures` fill the dialog through the picker now. **249/249.**
- **Data source (2026-09-18, nineteenth session)**: `dataSource` 14 (`previewSync` counts exactly the rows the save writes — changed with balance vs ageing-only, new, settled with amount, unchanged, untouched — and agrees with `mergeWithExistingFollowUps`; `describePreview`; `syncAge` wording; the review's four tiles, changes-first rows, honest re-aged label, tile filters, the *Update balances N rows* button handing over the merged book, Esc, "Nothing changes"; the page's health line, what-controls-what, the two actions, the fresh start Admin-only/red/last with clears-and-keeps, the check's reachable/next-sync line and *Review and update*, the unreadable-sheet alert, the non-official link, Excel mode with the folded table, the import's in-app question). **263/263.**
- **Polish (2026-09-18, twenty-first session)**: `useModal` 4 (initial focus, Tab wraps both ways, focus pulled back from outside, Esc closes and returns focus to the opener; Esc can be refused; stacked dialogs: the inner one takes Esc and hands focus back to its opener; the confirm dialog starts on Cancel). Browser-side: `scripts/tests/a11y-sweep.cjs` (read-only). **267/267.**
- **Final UI cleanup (2026-09-18, twenty-third session)**: `teamTemplates` 5 (the Team table shows each person's role, scope and rights in the one badge; the Admin is never offered for removal and the others are handed to the app's question; the company profile sits on its own tab and saves as a whole with GSTIN upper-cased; a template opens from its name and shows its first line; the empty state's one action). `useModal` re-pinned to the shell's `aria-labelledby`. **Total 272.**
- **Architecture cleanup (2026-09-18, twenty-fifth session)**: `metrics` 13 (the eight Today figures pinned as App computed them: credits skipped in ageing, each account with dues counted once by the date with defaulters on their own card, the list agrees with the boxes and a search finds anyone, commitments by today / week / all open, the team table buckets one person's spellings together and counts a collector's work, the cheque summary scoped to the person), `todayPages` 6 (the two Today pages rendered on their own: cards open Reports on the state they count, the team table only for whoever runs the team, the cheque button off when nothing is due, the personal list's rows with WhatsApp and a follow-up off for a role that cannot record one, the empty state's way back). **Total 291.**
- **T65 (2026-09-18, twenty-seventh session)**: `signOut` 2 — the first test that mounts `App` itself (Supabase client and repository mocked): the session is restored, the book and cheques arrive, Sign out is pressed, the login screen appears with no render error; and `chequeSummary` for a null user is the empty summary, never a throw. **Total 293.**
- **Final QA (2026-09-18, twenty-eighth session)**: `dateSeeding` 4 (the three dialogs show the local calendar day for a date stored at local midnight — T37), `roles` 7 (a Viewer reads and writes nothing; a Collector follows up and records cheques but cannot add, delete, export or reassign; a CRM never deletes), `adversarial` 9 (sheet garbage reads as zero, blank/#N/A names skipped, an empty Excel row is a visible "Unknown Company", dates either side of midnight, invalid dates, credits and NaN, regex characters in a search, two accounts with one name under a sync). **Total 313.**
- **Cheque page, second pass (2026-09-18, twenty-ninth session)**: `chequeRegister` rewritten to the three-list model, 29 (every state in exactly one list; the counts on the tabs; the most urgent first; the list with work in it opens first and an empty attention list says what is coming; the search and the folded filters; when-lines in plain words with a tag only where it adds something; one action per row with the rest in the menu, a cleared cheque with nothing to press; delete through the menu; bulk on the working lists only; the dialog's folded state control). **Total 315.**
- **Cheque page, third pass (2026-09-18, thirty-first session)**: `chequeRegister` rewritten to the refined-original model, 29 (one register in the register order; six tiles counting the narrowed rows and each listing exactly its count; the tile again or Show all; the search, bank and date selects narrowing tiles and list together; Today's `today` on Due today and an account's chip; a badge on every row with the date's note only where the badge does not say it; each state's actions and no other, edit and delete on every row; the viewer; the refused row; the named delete; the bulk bar's six and a selection trimmed to the rows listed; loading, empty, the tile with nothing in it, fifty rows). **Total 315.**
- **Mobile pass (2026-09-19, thirty-third session)**: no new unit tests — the batch is layout and loading, and the existing 315 cover the behaviour it must not change (they all still pass). Browser QA instead, all read-only with writes aborted: `m-flow.cjs` (the phone journeys at four widths), `m-tour.cjs` (every screen and dialog, touch targets, text sizes, input types), `m-overflow.cjs` (sideways scroll per page per width), `m-stress.cjs` (125 % text, landscape, reduced motion, the follow-up sheet's focus trap), `m-perf.cjs` (a throttled mid-range phone: bytes, first paint, tab switches, typing, scrolling), plus the existing `pdc-qa3`, `ux-qa`, `mgr-qa`, `src-qa` and the a11y sweep at phone and laptop widths.
- **Reports counts and follow-up wording (2026-09-19, thirty-fifth session)**: `managerReports` +3 (a state chip counts what the ageing chip and the search leave and opens exactly that many; an ageing chip counts what the state chip leaves; the search narrows tiles, chips and list together), and the wording tests now assert the badge is not repeated. **Total 318.**
- **Known gaps**: `useCollectionSync` (a hook with timers — testable with `renderHook` + fake timers in the R1 batch); `updateCustomers` (needs a fake Supabase client); the digest's scoping (server module, testable with fixture rows); Excel/master import mapping.


What exists in the repo
- No unit/integration test runner (no vitest/jest/playwright in `package.json`).
- Puppeteer scripts in `scripts/`: `smoke.cjs` (screenshots), `audit.cjs` (UI/UX audit light/dark), `interact.cjs` (functional click sweep), `contrast.cjs`, `tour.cjs`, with `signin.cjs` shared sign-in (credentials from env or `.deploy.local`). Static checks: `deadclasses.cjs`, `emptycontrols.cjs`. All run against a local dev server on :3000 signed in to the production Supabase project (read-only by convention).

Brought into the repo in Phase 0 (`scripts/tests/`, with a README)
- During recent feature work a set of puppeteer suites was written outside the repo; they were copied in verbatim as a safety step: `stock-test.cjs` (45 checks), `overview-test.cjs` (38), `compare-test.cjs` (54), `price-ui-test.cjs` (12 admin / 24 with a CRM), `crm-test.cjs` (9; creates and removes a throwaway CRM login through `/api/team`), `baddebt-test.cjs` (15), `book-filters-test.cjs` (25), `chips-test.cjs` (20), `phone-test.cjs` (38), plus an offline digest check. They assert internal consistency (counts add up, filters agree) rather than remembered numbers, so they survive the data changing daily.
- Proposal (Phase 13): give them one runner (`npm run test:browser`), and document the credential requirement. Add a small unit layer (vitest) for the pure domain rules in `types.ts` (`overdueAgeing`, `getCustomerPaymentRank`, `isBadDebt`, `getFollowUpCategory`, `chequeState`, `netRollUp`) and `services/liveStock.ts` (parser, `availabilityOf`, `isCritical`) — the rules money decisions depend on, and pure functions today.

Highest-value coverage gaps (preliminary)
1. Domain rules in `types.ts` — no tests at all.
2. Sheet import/reconciliation (`googleSheetService.ts`) — writes back to the book.
3. Permissions: client `can()` vs RLS.
4. Daily digest content per role.

## Production-safety classification (Phase 1, inspected before execution; nothing executed)

| Script | Class | Notes |
|---|---|---|
| `scripts/tests/stock-test.cjs`, `overview-test.cjs`, `compare-test.cjs`, `price-ui-test.cjs` (admin leg), `book-filters-test.cjs`, `chips-test.cjs`, `baddebt-test.cjs` | READ ONLY | filters, tabs, drawers, browser storage only |
| `scripts/tests/crm-test.cjs`; `price-ui-test.cjs` CRM leg | CONTROLLED MUTATION WITH CLEANUP | creates and removes a throwaway CRM login via `/api/team`; touches production `auth.users`/`profiles` |
| `scripts/tests/phone-test.cjs` | READ ONLY, fragile | presses the Admin bulk "Set follow-up" on "Select all" and relies on the confirm being dismissed — rewrite that step before running against production |
| `scripts/interact.cjs`, `smoke.cjs`, `audit.cjs`, `tour.cjs` | READ ONLY (likely) | no save/confirm-accept found; not exhaustively traced |
| `scripts/tests/write-payload-probe.cjs` (new) | READ ONLY by construction | Puppeteer request interception aborts every non-GET to `/rest/v1/*` and the app's mutating API routes before they leave the browser; the rows touched were re-read afterwards and unchanged. Use this pattern for any future "what would the app write" evidence. |
| none | UNSAFE FOR PRODUCTION | any future test of sync, reset, delete, bulk rank/reassign or the cheque lifecycle needs staging (Q6) |

## What a staging environment needs (not pursued this session)
1. A second Supabase project; run `supabase/schema.sql`; disable sign-up; create an Admin.
2. Seed: either a scrubbed copy of production (`customers`, `pdc_cheques`, `customer_activity`, `profiles` with fake emails) or a generated book of ~4,000 rows with realistic Dr/Cr and ageing.
3. `.env.staging` + a `TIMELY_ENV=staging` switch in `scripts/signin.cjs` and `npm run dev`, and a Vercel preview project pointed at it.
4. Only then: write the S0 tests (sync/reconciliation, write-back, reset) and the destructive-edge-case tests.
