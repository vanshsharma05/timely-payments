# 16 — CHANGELOG (audit)

Changes made under this audit, newest first. Feature work before the audit is in `git log`.

| Date | Batch | Files | Change | Validation |
|---|---|---|---|---|
| 2026-09-18 18:40 IST | **DEPLOYED**: T65 fix (`d6413f5`) | `services/metrics.ts` (`chequeSummary` empty for a null user), `tests/signOut.dom.test.tsx` (new: mounts App, loads the book and cheques, presses Sign out, expects the login screen; the summary for a null user) | Pushed both branches to `d6413f5`; `npx vercel deploy --prod --yes` → `dpl_EumThvc35QxXfSMeTZ7uS21PnPnm` (Ready, aliased). Gate: clean tree, tsc clean, **293/293**, build clean (567 kB, no size warning), checks unchanged. | All 13 JS chunks byte-identical to the local build (hash names aside); CSS as always (+10 unused utilities). **Sign out on production: the login screen at once, 0 page errors** (was a blank page); a real reload keeps showing the login form; signing in again works; a reload while signed in still restores the session and honours the hash. Smoke 12/12; payload probe (10, all aborted) and status-contract probe (4) identical to the previous records. Read-only DB check: 4,029 customers · 153 cheques · 630 activity · 0 backups — the two newest cheques were recorded by team members at 18:36–18:39 IST (their own live use); nothing from the QA account. |
| 2026-09-18 18:15 IST | **DEPLOYED**: Phase 8 architecture cleanup (`ca557a1`…`30c1c36`) | — | Pushed both branches to `30c1c36`; `npx vercel deploy --prod --yes` → `dpl_HGVfSsfuyCvRLADszFQjCzyCswAK` (Ready in 46 s, aliased). Gate: clean tree, tsc clean, 291/291, build clean (567 kB main, no size warning), checks unchanged. | All 13 JS chunks reachable from the live entry byte-identical to the local build (hash names aside); the live CSS carries the same 10 unused Tailwind utilities as every prior deploy and lacks nothing. Smoke 12/12 (0 writes). The four workflow QA scripts against production (CRM at 1366/390, Manager/Reports, cheques at 1366/390, Data source at 1366/390): 0 findings, every write aborted, Fresh Start's button still disabled until the phrase. Payload probe (10 requests, all aborted) and status-contract probe (4) identical to the morning's production records. a11y sweep at 1366: every screen, dialog and question 0/0/0, focus kept, Esc closes. Navigation/session script (scratchpad `nav-session-qa.cjs`): sign-in lands on Today; a real reload restores the session and honours the hash; every `#tab` opens its screen and an unknown hash falls back to Today; tab clicks update the hash with no history entries stacked; "Review cheques" opens the register on today's cheques (the Manager navigation that was broken); "Record a cheque" on Today opens the dialog; the app bar searches stock on Live stock and customers elsewhere; Team lists 18, Templates 2; sign out → a reload shows the login form → signing in again works. **Found, not a regression (identical on the previous build `f193814`, present since the cheques batch): pressing Sign out blanks the page** — `chequeSummary` (`todayPdcMetrics` before) calls `isResponsibleFor(currentUser!)` with a null user while the cheques are still in state; a reload shows the login form and the session is signed out. Recorded as T65; one-line guard, not applied (no production-code change in this task). Read-only DB check after testing: 4,029 customers · 151 cheques · 630 activity · 0 backups · 18 profiles · 2 templates; the one new cheque (18:32 IST) and two edits (18:28–18:29) were recorded by team members through the new build — the team's own live use, and the first production writes through the refactored persistence; nothing from the QA account. |
| 2026-09-18 | Phase 8: App.tsx architecture cleanup (twelve commits `ca557a1`…`1fa40f3`) | `services/metrics.ts` (new, 7 pure figures + `CrmStat`), `services/excel.ts` (parser, template, both exports), `services/googleSheetService.ts` (official addresses; `getOutstandingForUser` removed), `hooks/` (new: `useSession`, `useTab`, `usePersistence`, `useDataSource`, `useCustomers`, `useCheques`, `useTeam`, `useTemplates`, `useWorklistFilters`), `components/pages/CompanyToday.tsx` + `PersonalToday.tsx` (new), `components/shell/ShellBanner.tsx` (new), `types.ts` (`DEFAULT_TEMPLATE`), `components/ui/format.ts` (`startOfToday`), `CrmPerformanceTable.tsx` (type from metrics), `Icons.tsx`/`NavIcons.tsx` (15 unused icons out), `ChequeState.tsx`/`useSupabaseSync.ts`/`types.ts` (3 unused helpers out), `App.tsx` 3,107 → 771 lines; tests `metrics.test.ts` (13), `todayPages.dom.test.tsx` (6) | No rule, formula, schema, RLS or permission change. Two confirmed bugs fixed: a Manager's/Viewer's "open in cheques" went nowhere (two tab states, one read); a CRM's AI summary named the placeholder company. See 09 §D. | tsc clean; **291/291**; build clean (567 kB main, gzip unchanged, no size warning); checks unchanged; a11y sweep at 1366 all zero; the four workflow QA scripts 0 findings; smoke 12/12; both probes identical to the production records, every write aborted. **Committed locally, NOT deployed.** |
| 2026-09-18 13:24 IST | **DEPLOYED**: final UI cleanup (`f193814`) | — | Pushed both branches to `f193814`; `npx vercel deploy --prod --yes` (the first attempt printed the CLI's error block without creating a deployment, as before; the retry built in 48 s) → `dpl_3bkoSiywjZb3sYzvcgcC5HeAJnHc` (Ready, aliased to timely-payment.vercel.app). Gate before: clean tree, tsc clean, 272/272, build clean (563 kB main, no size warning), checks unchanged. | All 14 chunks reachable from the live entry byte-identical to the local build (hash names aside); the live CSS carries the same 10 unused Tailwind utilities from Vercel's build cache and lacks nothing. Smoke 12/12 (0 writes attempted). Payload probe: 10 mutating requests captured, all aborted, record identical to the previous production record (timestamps aside); status-contract probe: 4 captured, all aborted, identical. a11y sweep on the live site at 1366×768 incl. the three in-app questions and the Company profile tab: every screen and dialog 0 overflow / 0 low-contrast / 0 unnamed, focus never left a dialog, Esc closed each. Team, Company profile, Templates, Alerts, the user and template dialogs and the delete/remove questions screenshotted live — as in local QA. Read-only DB check after testing: 4,029 customers · 150 cheques · 620 activity · 0 backups · 18 profiles · 2 templates; last writes 12:26 IST (customers) and 12:19 IST (cheques) — the team's own, before the deployment; nothing written during testing. |
| 2026-09-18 | Phase 4 batch 6: the final UI consistency cleanup (T57, T58, T53, Team/Templates/Alerts, T55) | `components/ui/DialogShell.tsx` (new), `components/ui/fields.ts` (new), `components/ui/ConfirmDialog.tsx` (on the shell), `components/ui/Primitives.tsx` (`destructive` variant), every `*Modal.tsx` on the shell with `Button` footers, `components/TeamView.tsx` (new, lifted from App.tsx), `components/TemplatesView.tsx` (new), `components/CompanyProfileView.tsx` (rebuilt on `FIELD`/`LABEL`), `components/UserModal.tsx` (body on tokens, lists for role/scope/rights), `App.tsx` (one `ask` state + root `ConfirmDialog`; delete customer / remove member / delete template ask in the app; Team and Templates tabs replaced by the views; the Settings wrapper frames only Reports; −184 lines), `CustomerDashboardView.tsx` + `ReportsView.tsx` (bulk-date question), `FollowUpModal.tsx` (remove-contact question; footer on `Button`), `scripts/tests/a11y-sweep.cjs` (the questions and the company tab), tests `teamTemplates.dom.test.tsx` (new), `useModal.dom.test.tsx` | One dialog shell and one button language in every dialog; no browser confirms left; Team, Templates and Alerts in the book's chrome; no card-in-card on the Setup pages. No rule, formula, permission, schema or backend change. | tsc clean; **272/272**; build clean (563 kB main, no size warning); checks unchanged; a11y sweep at 1366/1024/390 all zero; the four workflow QA scripts 0 findings with every write aborted. **Committed locally, NOT deployed.** |
| 2026-09-18 12:14 IST | **DEPLOYED**: polish batch (`0388cc7`) | — | Pushed both branches to `0388cc7`; `npx vercel deploy --prod --yes` (first attempt returned a CLI error before creating anything; second succeeded) → `dpl_EkySEM3wb7JmULMYQSyi3eZiVFrg` (Ready, production, one new deployment). **All 14 JS chunks** reachable from the live entry (index, react, sheets, ReportsView, PdcChequesView, LiveStockView, DataSourceView, AiReportModal, UserModal, TemplateModal, ChangePasswordModal, ResetConfirmModal, SyncReconciliationModal, ConfirmDialog) are byte-identical to a build of `0388cc7` once the hash strings in file names and import specifiers are set aside (they differ because Vercel's CSS hash differs: the same 10 benign unused rules as every deploy, none missing). At sign-in the browser fetches only `index` + `react` (697 kB, no `sheets`); Reports, Cheques, Live stock and Data source each fetch their chunk on first open and render (312–2,190 ms). | Gate: clean tree, tsc clean, 267/267, build clean with no size warning, checks unchanged. Smoke 12/12 (writes aborted, 0 attempted). Probes (every write aborted): no-change edit 0; phone `[contact_number]`; owner `[crm_owner_id]`; urgent `[is_urgent,last_follow_up_on]`; sync review 683 rows (the team had synced the sheet themselves at 08:37 IST, so 0 money PATCHes this time — the book already matched — + the `app_settings` upsert); clock +1 day identical. `a11y-sweep` on the live site at 1366×768: 0 overflow, 0 AA failures, 0 unnamed controls, all 8 dialogs trapped + Esc. The four workflow QA scripts at 1366×768 on the live site: 0 findings (one script artefact: an 80-char slice cut "Date passed" to "Date pass"). **Production data**: nothing written by testing — every mutating request from the test browsers was aborted (counts logged). During the window the team was using the app: a real sync at 08:37 IST (before the deploy; 230 rows, 2 new customers → 4,029), cheque #405987 marked Cleared at 12:17 IST and #035225 put back to Pending at 12:19 IST — both from the register by a signed-in user while no test was on that page; 0 customers and 0 activity entries written since the deploy; 0 backups; 0 WhatsApp-trace rows. |
| 2026-09-18 | Phase 4 batch 5: consistency, accessibility, performance | `components/ui/useModal.ts` (new; `useEscape.ts` removed), every `*Modal.tsx` + `ConfirmDialog` + Live stock's two dialogs (hook, role/aria, `data-autofocus`), `services/excel.ts` (new: SheetJS on demand) + the four exports/upload, `vite.config.ts` (function `manualChunks`), `App.tsx` (lazy tab views and setup dialogs with `Suspense`; Setup headings/buttons; templates empty state), `ReportsView.tsx` (lazy AI report), `components/shell/AppShell.tsx` (search draft, 180 ms), sentence-case labels in `CustomerEditModal`, `CustomerDashboardView`, `UserModal`, `TemplateModal`, `AiReportModal`, `FollowUpModal`; contrast fixes in `PhoneAccountRow`, `LiveStockView`, `AiReportModal`, `BalanceAmount`; `scripts/tests/a11y-sweep.cjs` (new), `status-contract-probe.cjs` (case-insensitive press), `scripts/audit.cjs`/`interact.cjs` (labels); tests | Every dialog traps focus, closes on Esc, returns focus; one button language and sentence case; zero AA contrast failures; first load −618 kB and no chunk warning; app-bar typing 5–9 ms per key. No rule, formula, permission or schema change. | tsc clean; build clean (no size warning); checks unchanged; **267/267**; the four workflow QA scripts at their sizes + the a11y sweep at 1366/1024/390: 0 findings; smoke 12/12 and both probes identical to the records against the local build. **Committed locally, NOT deployed.** |
| 2026-09-18 02:29 IST | **DEPLOYED**: Data source batch (`ab00816`, `1e59cc4`) | scratchpad smoke script: the Fresh Start check now looks for "Fresh start — recovery only" / "Start a fresh start" | Pushed both branches to `1e59cc4`; `npx vercel deploy --prod --yes` → `dpl_FKcvzMKdrfKVgZ1d3qL4zmAPoNCT` (Ready, production, one new deployment). The page's script `index-Ig6-w3iU.js` byte-identical (0 differing bytes) to a build of `1e59cc4`; CSS +10 unused rules as before, none missing. | Gate: tsc clean, 263/263, build clean, checks unchanged. Smoke 12/12 on the live site (writes aborted, none attempted; first run 11/12 only because the script still looked for the old section title). Probes (every write aborted): no-change edit 0; phone `[contact_number]`; owner `[crm_owner_id]`; urgent `[is_urgent,last_follow_up_on]`; sync 684 reviewed → 10 money-only PATCHes + the `app_settings` upsert; clock +1 day identical — all as recorded. Data source QA on the live site at 1366×768: 0 findings — *Check the sheet* read the sheet (684 rows, updated till 13-Sep-2026, next sync 9 change · 1 settled · 675 unchanged) with 0 requests attempted; *Review and update* opened the review on it; *Sync balances* → Cancel wrote nothing; the fresh-start section is Admin-only and its dialog opened with *Reset the book* disabled and was cancelled; no RPC called. Production unchanged: 4,027 customers, 150 cheques, 615 activity entries, 0 backups, 0 WhatsApp-trace rows, 2 templates, `last_sync_time` 14 Sept; newest customer/cheque writes 17 Sept before the deploy; probe accounts' `updated_at` 2026-09-14. |
| 2026-09-18 | Phase 4 batch 4: the Data source page | `components/DataSourceView.tsx` (new), `services/syncPreview.ts` (new: `previewSync`, `describePreview`, `syncAge`), `components/SyncReconciliationModal.tsx` (rebuilt on the preview), `App.tsx` (the page extracted; `sheetCheck` + `handleCheckSheet`/`handleReviewCheck`; the import's `confirmed` option; the post-sync message says what changed), tests | Health and the everyday sync first in plain words; "Check the sheet" says what the next sync will do without writing; the review counts exactly what the save writes (9, not 684); the fresh start Admin-only, red-bordered, last, naming what it clears and keeps. No rule, schema or permission change. | tsc clean; build clean; checks unchanged; **263/263**; local QA at 1366×768, 1440×900, 1024×768, 390×844 with every write aborted and the reset never confirmed: 0 findings; the write-payload probe against the local build identical to the records. **Committed locally, NOT deployed.** |
| 2026-09-17 23:21 IST | **DEPLOYED**: cheque batch (`46e7e21`, `4efa3e1`) | — | Pushed both branches to `4efa3e1`; `npx vercel deploy --prod --yes` → `dpl_Ev7XczkMQHJCBsKXRnAKMNKzXkDM` (Ready, production, one new deployment). The page's one script `index-DsA5F7Qi.js` is byte-identical (cmp, 0 differing bytes) to a build of `4efa3e1`; the CSS carries the same 10 unused utility rules as the previous deploy (Tailwind on Vercel scanning built files), no rule missing. | Gate: tsc clean, 249/249, build clean, checks unchanged. Smoke 12/12 on the live site (PDC check now "50 rows" — paging, as expected; writes aborted, none attempted). Probes (every write aborted): no-change edit 0; phone `[contact_number]`; owner `[crm_owner_id]`; urgent `[is_urgent,last_follow_up_on]`; sync 684 reviewed → 10 money-only PATCHes + the `app_settings` upsert; clock +1 day: `[is_urgent,last_follow_up_on]` / `status:"Completed"` / `[follow_up_date]` — identical to the records. Cheque workflow QA on the live site at 1366×768: 0 findings (the date had rolled to 18 Sept during the run: yesterday's six due cheques sat at the top as *Date passed · Yesterday*, seven *Due today* under them — the order doing its job). Production unchanged: 4,027 customers, 150 cheques, 615 activity entries, 0 backups, 0 WhatsApp-trace rows; newest customer write 12:29 IST and newest cheque write 18:47 IST, both before the deploy; the cheque the QA pressed *Clear* on (#405987) is still Pending with no cleared date; probe accounts' `updated_at` 2026-09-14. |
| 2026-09-17 | Phase 4 batch 3: the cheque register + dialog | `components/ui/ChequeState.tsx` (new: labels, hints, badge, order, `sortCheques`, `stateOf`), `components/ui/ConfirmDialog.tsx` (new), `components/ui/format.ts` (`chequeWhen`), `components/PdcChequesView.tsx` (rebuilt: filter card, seven-cell strip, state-keyed filters, order, paging, state-fit actions, hover-only delete behind a named question, row-level not-saved, loading/empty states), `components/PdcModal.tsx` (rebuilt: typed customer picker, no silent defaults, bank suggestions, dated wording, segmented status, duplicate warning), `components/FollowUpModal.tsx` (shared badge in the Cheques fold), `App.tsx` (both register sites get `onOpenCustomerFollowUp`, `loading`, `unsaved`; the dialog gets the register and roster; a no-op status press is ignored); tests | The cheque workflow in the book's chrome and words; what needs action first; deleting asks with the cheque named; a dialog that guesses nothing. No cheque rule, permission or schema change; two small bugs fixed (Upcoming chip listed Pending; Clear on a cleared cheque re-stamped `clearedDate`); *added by* survives an edit. | tsc clean; build clean; checks unchanged; **249/249**; local QA at 1366×768, 1440×900, 1024×768 and 390×844 with every write aborted: 0 findings. **Committed locally, NOT deployed.** |
| 2026-09-17 22:21 IST | **DEPLOYED**: Manager/Reports batch (`d1893e3`, `4ad33cd`) | — | Pushed both branches to `4ad33cd`; `npx vercel deploy --prod --yes` → `dpl_8MdpwitnETxviwA9XWRvupPzg7th` (Ready, production, one new deployment). Live JS bundle byte-identical (cmp) to a build of `4ad33cd`; live CSS carries 12 extra unused utility rules (`.m-1`, `.table-cell`, `.hidden\!`, …) that Tailwind on Vercel emitted from tokens it scanned — none used by the markup, no rule changed; local `dist/` is gitignored so the local build omits them. | Gate: tsc clean, 222/222, build clean, checks unchanged. Smoke 12/12 on the live site (writes aborted, none attempted). Probes on the live site (every write aborted): no-change edit 0; phone `[contact_number]`; owner `[crm_owner_id]`; urgent `[is_urgent,last_follow_up_on]`; sync 684 reviewed → 10 money-only PATCHes + the `app_settings` upsert; clock +1 day: `[is_urgent,last_follow_up_on]` / `status:"Completed"` / `[follow_up_date]` — identical to the records. Manager workflow QA on the live site at 1366×768: 0 findings (Savia → 73 · Unattended 50 · Accounts 72 exactly; band 191; Needs attention 135; Overdue 115 with no stale filter). Production unchanged: 4,027 customers, 150 cheques, 615 activity entries, 0 backups, 0 WhatsApp-trace rows, newest customer write 12:29 IST, newest cheque write 18:47 IST (the team's), `last_sync_time` 14 Sept; probe accounts' `updated_at` 2026-09-14. |
| 2026-09-17 | Phase 4 batch 2b: T51 (owner's decision) | `components/ReportsView.tsx` (`working` category + chip; a defaulter is out of *Completed* too), `components/CrmPerformanceTable.tsx` (the count drills to `working`), test | A pressed "Accounts with dues" count opens exactly the accounts it counted. No formula change; 0 production accounts affected by the *Completed* alignment on the day. | tsc clean; **222/222**; build clean. |
| 2026-09-17 | Phase 4 batch 2: the Manager's Today + Reports; Live-stock search fix | `services/search.ts` (new), `App.tsx` (`stockSearch`; `reportCrm`/`reportAgeing`; `openReport`; Today reordered; ageing bands are buttons; both Reports render sites wired), `components/ReportsView.tsx` (filter card, attention tiles, ageing card, book-vocabulary chips, `unattended`/`Needs attention`, shared search, `initialAgeingFilter`, paging, `followUpWhen` cell, *Follow up*), `components/CrmPerformanceTable.tsx` (`onSelectCrm`, `drillable`, most-unattended first, headers), `components/ui/format.ts` (`followUpWhen` moved here), `components/CustomerDashboardView.tsx` (import; "All ageing"), tests | The customer search no longer filters Live stock (own term). Reports rebuilt in the book's chrome and words; every number on Today opens Reports on exactly that list; nothing stale carries over. No formula, rule, permission or schema change. | tsc clean; build clean; checks unchanged; **221/221**; local QA at 1366×768, 1440×900, 1024×768 with every write aborted: 0 findings. **Committed locally, NOT deployed.** |
| 2026-09-17 20:46 IST | **DEPLOYED**: CRM UX batch (`95adcbf`, `6a33c29`) | probes: `scripts/tests/write-payload-probe.cjs`, `scripts/tests/status-contract-probe.cjs` (the row button is now "Follow up" and the dialog's button "Save follow-up"; matched accordingly) | Pushed both branches to `6a33c29`; `npx vercel deploy --prod --yes` (first attempt returned no URL, second succeeded) → `dpl_HeLAK1WcbkKNHo9N8rsunT98YPq5` (Ready, production). Live bundles byte-identical (cmp) to a build of `6a33c29`; new markers present. | Gate: tsc clean, 196/196, build clean, checks unchanged. Smoke 12/12 (writes aborted, none attempted). Probes on the live site (every write aborted): no-change edit 0; phone `[contact_number]`; owner `[crm_owner_id]`; urgent `[is_urgent,last_follow_up_on]`; sync 684 reviewed → 10 money-only PATCHes + the `app_settings` upsert; clock +1 day: `[is_urgent,last_follow_up_on]` / `status:"Completed"` / `[follow_up_date]` — identical to the records. CRM workflow QA on the live site at 1366×768: 0 findings. Production unchanged: 4,027 customers, 150 cheques (6 recorded by the team earlier today), 615 activity entries, 0 WhatsApp-trace rows (every launch was aborted), newest customer write 12:29 IST; probe accounts' `updated_at` 2026-09-14. **Smoke-script note, not a regression:** the search is one term for the whole app now, so a term typed in the book also filters Live stock until cleared — the app bar shows it with a ✕. |
| 2026-09-17 | Phase 4 batch 1b: the four UX decisions + visual QA | `components/FollowUpModal.tsx` (collected → *Keep as collected*; WhatsApp trace; heading), `components/CustomerDashboardView.tsx` (one search; `max-xl` column folds for 1024 px), `components/WhatsAppReminderModal.tsx` (trace, "Open WhatsApp"), `services/whatsappTrace.ts` (new), `services/useSupabaseSync.ts` (`sentence`), `components/CustomerEditModal.tsx` / `PdcModal.tsx` (sentence), `App.tsx` (`onGlobalSearch`, `currentUser` to the WhatsApp dialog); tests | The owner's four decisions; the CRM workflow checked at 1366×768, 1440×900, 1024×768 and 390×844 with zero findings after three fixes. | tsc clean; build clean; **196/196**. **Committed locally, NOT deployed.** |
| 2026-09-17 | Phase 4 batch 1: CRM daily workflow | `components/FollowUpModal.tsx` (restructured: header context, outcome first, four folded sections, prev/next, Esc, "Save follow-up"), `components/ui/Disclosure.tsx` (new), `components/ui/useEscape.ts` (new), `components/CustomerDashboardView.tsx` (toolbar, foldable overview, rows, follow-up wording, hover delete, empty state, `onVisibleRowsChange`, `followUpWhen`), `components/CustomerEditModal.tsx` / `PdcModal.tsx` / `WhatsAppReminderModal.tsx` (Esc), `App.tsx` (list-aware open, prev/next); tests: `tests/crmWorkflow.dom.test.tsx` | Cleaner hierarchy, faster loop, same rules and permissions. See 06 "Phase 4, batch 1". | tsc clean; build clean; **190/190**; screenshots at 1366×768 before/after (scratchpad). **Committed locally, NOT deployed** — awaiting the owner's visual review. |
| 2026-09-17 15:33 IST | **DEPLOYED**: reliability batch (`3684dc1`, `7e1136a`) | `scripts/tests/write-payload-probe.cjs` (closes a dialog a refused save keeps open; waits for the review itself) | Pushed both branches to `7e1136a`; `npx vercel deploy --prod --yes` → `dpl_GfxvXMy7gxHCRTbHSBLJeAkxQn1o` (Ready, production). Live bundles byte-identical (cmp) to a build of `7e1136a`; new markers present. | Gate: tsc clean, 174/174, build clean, checks unchanged. Smoke 12/12 (writes aborted, none attempted). Probes on the live site (every write aborted): no-change edit 0; phone `[contact_number]`; owner `[crm_owner_id]`; urgent `[is_urgent,last_follow_up_on]`; sync 684 reviewed → 10 money-only PATCHes (avg 88 B, no app-owned column) + the `app_settings` upsert; clock +1 day: `[is_urgent,last_follow_up_on]` / `status:"Completed"` / `[follow_up_date]`. **Probe finding, not an app regression:** with writes aborted the new behaviour keeps the edit and follow-up dialogs open (a refused save), which the old probe did not expect — it now closes them. Production unchanged: 4,027 / 144 / 615 / 0 backups; newest customer write 12:29 IST; probe accounts' `updated_at` 2026-09-14. |
| 2026-09-17 | Retry idempotency check (before deploying batch 6) | `services/ids.ts` (new), `services/repository.ts` (`addActivity` takes a client id; a duplicate-key answer on the retry is read back as the saved entry), `components/CustomerActivityPanel.tsx` (one id per composed entry until saved), `components/CustomerEditModal.tsx` and `components/PdcModal.tsx` (a new record's id fixed per dialog, not per Save), `App.tsx` (`handleSavePdc` replaces-or-adds), `services/refresh.ts` (`replaceOrAdd`); `tests/retryIdempotency.dom.test.tsx` | Every automatically retried write is repeatable: column updates and deletes by id; new customers, cheques and templates upserted under client ids. The three user-retried creates that could have doubled (a note/promise posted again after a lost answer; a new cheque or new customer saved again from the still-open dialog) now cannot. | tsc clean; build clean; **174/174** (7 new: server stores → client sees a network failure → retry → exactly one record, for activity/promise, new cheque, new customer, column update). **Committed locally, NOT deployed.** |
| 2026-09-17 | Reliability: honest saves + fresh tabs | `services/useSupabaseSync.ts` (flush / retry with backoff / onStatus / accept / forget / humanised reasons), `services/refresh.ts` (new: `mergeServerRows`), `components/SaveStatus.tsx` (new), `components/shell/AppShell.tsx` (`saveStatus` slot), `components/FollowUpModal.tsx`, `components/CustomerEditModal.tsx`, `components/PdcModal.tsx` (wait for the verdict; stay open on a refusal), `App.tsx` (handlers return outcomes; bulk actions report after the pass; persistent refused-write banner; `refreshBook` on focus/interval/reconnect/button); tests: `tests/saveFailures.dom.test.tsx`, `tests/refreshMerge.test.ts` | A refused save is shown where it happened and stays visible until saved; nothing says "saved" before the server accepted; refused writes retry on their own; long-open tabs re-read the book without touching unsaved work. | tsc clean; build clean; **167/167**; local browser probe with every write aborted (dialog stays open with the reason; one attempt; automatic retry at +5.2 s; Refresh keeps the unsaved row; Retry now). Production untouched. **Committed locally, NOT deployed.** |
| 2026-09-17 15:00 IST | Post-deploy verification of the fresh-start batch (`dpl_7mKQWQGhHHNKGnLcDe7Y13EgvHKq`) | docs only | Browser checks re-run once a valid Admin login was in `.deploy.local`. | Read-only smoke **12/12** (sign-in; book 61 rows; search; follow-up dialog open/close; edit dialog open/close; Reports 642; PDC 144; live stock 60; **Fresh Start section visible to the Admin, not pressed**; no page errors; 0 mutating requests attempted). Probes against the live site, every write aborted: no-change edit 0; phone `[contact_number]`; owner `[crm_owner_id]`; urgent `[is_urgent,last_follow_up_on]`; sync 684 reviewed → 10 money-only PATCHes (avg 88 B, no app-owned column); clock +1 day: `[is_urgent,last_follow_up_on]` / `status:"Completed"` / `[follow_up_date]` — identical to the earlier records. Production unchanged: 4,027 customers, 144 cheques, 615 activity entries, `book_backups` 0 rows, newest customer write 12:29 IST; probe accounts' `updated_at` 2026-09-14. No reset executed. |
| 2026-09-17 14:45 IST | **DEPLOYED**: fresh start (`7b1ec3c`, `3fbe7f7`) | `supabase/reset.sql` applied to the project first (via the management API's SQL query, the SQL editor's equivalent; Postgres 17.6); then push of both branches to `3fbe7f7`; `npx vercel deploy --prod --yes` → `dpl_7mKQWQGhHHNKGnLcDe7Y13EgvHKq` (Ready, production) | Installed `book_backups`, `reset_book()`, `restore_book_backup()`; no reset was run (`book_backups` has 0 rows; 4,027 customers, 144 cheques, 615 activity entries unchanged). | Read-only catalog checks: both functions present, `security definer`, Admin check in the body, execute granted to `authenticated` and not `anon`, `book_backups` RLS on with `is_admin()` read policy. Live bundles byte-identical (cmp) to a build of `3fbe7f7`; markers present. `/api/health` 200. **Browser smoke test and interception probes could not run**: the Admin login in `.deploy.local` stopped working — Supabase Auth returns invalid credentials; the account's auth record was updated at 13:21 IST (after the last successful sign-in at 12:19 IST), i.e. the password was changed. Not a deployment fault; to be re-run once a valid Admin login is supplied. |
| 2026-09-17 | SEC3 fresh start made safe | `supabase/reset.sql` (new: `book_backups`, `reset_book()`, `restore_book_backup()`), `supabase/schema.sql` (pointer), `services/reset.ts` (new: plan, RPC call, backup file), `components/ResetConfirmModal.tsx` (new), `App.tsx` (handler: read sheet → plan → dialog → one RPC → reload from server), `services/repository.ts` (`pdcToRow` exported); tests: `tests/pglite.ts`, `tests/resetPlan.test.ts`, `tests/resetSql.test.ts`, `tests/resetConfirmModal.dom.test.tsx`; dev dependency `@electric-sql/pglite`; docs incl. DEPLOYMENT.md | One transaction with a snapshot; no deletes or re-ids; owners/collectors/contacts/threads kept; unlisted accounts settled; fail-closed on the sheet; counts + backup + typed phrase. | tsc clean; build clean; **146/146**; local flow probe against the real book with every write aborted: 0 requests before confirmation, exactly one `rpc/reset_book` on confirm (aborted), production re-read unchanged. **Committed locally, NOT deployed** — `supabase/reset.sql` must be applied to the project before the client goes live. Q8 decided the same day (Admin only; owners/collectors kept; history kept) and applied: UI gate + handler check + function role check + backup-table policy; 3 more tests (149). |
| 2026-09-17 11:00 IST | Post-deploy verification (re-run) | docs only | After the owner's own push/deploy: `npx vercel ls --prod` still shows `dpl_8NB7JLzJoSuCKsecBwz7WiycZBgJ` as current (no newer deployment); `origin/main` = `origin/restore-and-fix` = `6e81948` (app source identical to `de2d1c5`). | Live chunks byte-identical to a fresh `vite build` of the tree; smoke 11/11 (0 mutating requests attempted); write-payload probe: no-change 0, phone `[contact_number]`, owner `[crm_owner_id]`, urgent `[is_urgent,last_follow_up_on]`, sync 684 reviewed → 10 money-only PATCHes (avg 88 B, no app-owned column); status-contract probe (clock +1 day): `[is_urgent,last_follow_up_on]` / `status:"Completed"` / `[follow_up_date]`; probe accounts' `updated_at` 2026-09-14 unchanged; newest production write 05:15:07Z (before the deploy). No destructive action. |
| 2026-09-17 | **DEPLOYED**: Option A + Option B + status contract (`ee61d6f`, `2afb5d8`, `de2d1c5`) | `scripts/tests/write-payload-probe.cjs`, `scripts/tests/status-contract-probe.cjs` (take `PROBE_BASE`), `scripts/tests/README.md`, docs | Pushed `restore-and-fix` and `restore-and-fix:main` (both at `de2d1c5`); `npx vercel deploy --prod --yes` → `dpl_8NB7JLzJoSuCKsecBwz7WiycZBgJ` (10:49:47 IST, Ready, production). Live bundles byte-identical (cmp) to `npm run build` of the clean `de2d1c5` tree — all four chunks. | Pre-deploy gate: tsc clean, 109/109, build clean, checks unchanged. Production smoke (writes aborted, none attempted): sign-in, book 61 rows, search, follow-up dialog open/close, edit dialog open/close, Reports 642 rows, PDC 144 rows, live stock 60 rows, no page errors — 11/11. Probes against the live site (every write aborted): no-change edit 0 requests; phone `[contact_number]`; owner `[crm_owner_id]`; urgent `[is_urgent,last_follow_up_on]`; balance sync 684 rows reviewed → 10 money-only PATCHes (avg 88 bytes, no app-owned column) + the sync's `app_settings` upsert; clock +1 day: urgent `[is_urgent,last_follow_up_on]`, collected `status: "Completed"`, edit-dialog date `[follow_up_date]`. The three probe accounts' `updated_at` unchanged (2026-09-14). No destructive action; no reset. |
| 2026-09-17 | Status contract (final) | `types.ts` (`getFollowUpCategory` no longer falls back to the stored word), `components/FollowUpModal.tsx` (no derived word; `Pending` only to reopen), `components/CustomerEditModal.tsx` (`statusFor` removed; `Pending` on create / to reopen), `App.tsx` (bulk date: `Pending` only to reopen); tests: new `tests/statusContract.dom.test.tsx`, re-pinned `derivedStatus` / `customerEditModal` / `syncFlows`; new `scripts/tests/status-contract-probe.cjs` | `customers.status` is written only as `Completed` (collected) or `Pending` (reopened); no follow-up action persists a derived word. | tsc clean; build clean; 109/109; probe (clock moved to next day, all writes aborted): urgency → `[is_urgent,last_follow_up_on]`, collected → `status: "Completed"`, edit-dialog date → `[follow_up_date]`; production re-read unchanged (newest write still 2026-09-17T01:03Z). **Committed locally, not pushed or deployed** (with Options A and B). |
| 2026-09-17 | R1-C Option B | `types.ts` (`followUpStatusOf`, `FollowUpCategory`), `services/googleSheetService.ts` (`processStatuses` no longer writes status), `App.tsx` (export column, priority filter, status filter, bulk-date word), `components/CustomerDashboardView.tsx`, `components/ReportsView.tsx`, `components/AiReportModal.tsx` (readers derive); tests: new `tests/derivedStatus.dom.test.tsx`, re-pinned `money.test.ts` / `syncFlows.test.ts` | Date passage changes the reading, never the row; no automatic status PATCHes. | tsc clean; build clean; 93/93; checks unchanged; probe (clock moved to next day, one urgency toggle, all writes aborted): 90 requests before → 1 after; production rows re-read unchanged. **Committed locally, not pushed or deployed** (Option A is also still local). |
| 2026-09-17 | R1 Option A | `services/useSupabaseSync.ts`, `services/repository.ts`, `App.tsx` (adapter block only), `scripts/tests/write-payload-probe.cjs`; new `tests/customerRowDiff.test.ts`, `tests/updateCustomerColumns.test.ts`, `tests/useCollectionSync.dom.test.tsx`, `tests/syncFlows.test.ts` | Customer writes carry only the columns that changed since this tab last saved the row; the baseline advances per row on success. Whole-row `updateCustomers` removed. | tsc clean; build clean; 77/77 tests; check:classes/check:empty unchanged; interception probe (all writes aborted): no request for a no-change Save, `[contact_number]`, `[crm_owner_id]`, `[is_urgent,last_follow_up_on]`, balance sync 684 reviewed → 10 money-only PATCHes; three production rows re-read unchanged. **Committed locally, NOT pushed or deployed** (session rule). |
| 2026-09-17 | C1 fix | `components/CustomerEditModal.tsx`; new `vitest.config.ts`, `tests/fixtures.ts`, `tests/money.test.ts`, `tests/customerEditModal.dom.test.tsx`; `package.json` (scripts + 4 dev deps), `package-lock.json`; `README.md` (Checks) | The edit dialog keeps every field it does not own; owner, follow-up date/status and money are written only when actually changed. Unit-test layer added. | tsc clean; `npm run build` clean; 36/36 tests (11 dialog cases red before the fix); interception probe: a no-change Save now sends nothing, collector intact; the two production rows re-read unchanged; check:classes/check:empty unchanged. Commit `a0f0a31`, pushed to `restore-and-fix` and `main`, production deploy Ready (Vercel CLI, from that commit). |
| 2026-09-17 | Validation | `docs/product-audit/00, 05, 06, 11, 12, 13, 16, 18, 19`; `scripts/tests/write-payload-probe.cjs` | R1 confirmed with captured PATCH bodies; `processStatuses` traced; SEC3 traced to the database with production counts; SEC1/2/4/5 reclassified; C1 (edit dialog corrupts money/collector) found; decision briefs Q7–Q14; journeys J1–J16; UX U17–U23. No application code changed; every mutating request in the probe was aborted. | two rows re-read after the probe: unchanged |
| 2026-09-16 | Phase 1–2 | `docs/product-audit/03, 04, 06, 09, 11, 12, 13, 15, 18, 19, 00` | Product model, permission matrix, user types, App.tsx map, preliminary UX/security/reliability findings, test-safety classification. No application code changed. One read-only call to the Supabase management API to confirm `disable_signup`. | docs only |
| 2026-09-16 | Phase 0 | `docs/product-audit/*`, `scripts/tests/*` | Created the audit system, repository map, baseline, ledger; copied the nine browser suites into the repo verbatim. No application code changed. | typecheck / build / checks re-run for the baseline (recorded in 00-MASTER-STATUS.md) |
